[project]
name = "morphottention-workspace"
version = "0"
dependencies = []


[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    "pytest",
    "pytest-benchmark",
    "ruff",
    "mypy",
    "ninja"
]
